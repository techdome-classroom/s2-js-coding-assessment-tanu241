/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {  
    const stack =[];  
    const parenthesesMap = {
    '(': ')', 
    '{': '}', 
    '[': '}',  
    }; 

for(let i =0; i<s.length; )
    
};

module.exports = { isValid };


